apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pihole
  namespace: argo-cd
spec:
  project: default
  source:
    chart: pihole
    repoURL: https://mojo2600.github.io/pihole-kubernetes
    targetRevision: "2.24"
    helm:
      values: |
        persistentVolumeClaim:
          enabled: true

        serviceWeb:
          loadBalancerIP: 10.0.0.61
          annotations:
            metallb.universe.tf/allow-shared-ip: pihole-svc
          type: LoadBalancer

        serviceDns:
          loadBalancerIP: 10.0.0.61
          annotations:
            metallb.universe.tf/allow-shared-ip: pihole-svc
          type: LoadBalancer
  destination:
    server: "https://kubernetes.default.svc"
    namespace: pihole
  syncPolicy:
    syncOptions:
    - CreateNamespace=true